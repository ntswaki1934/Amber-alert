document.addEventListener("DOMContentLoaded", () => {
    // You can add your JavaScript code here to dynamically update the alert message
    const alertMessage = document.getElementById("alert-message");
    alertMessage.textContent = "Child missing: Jane Doe, age 7, last seen in Central Park.";
});
